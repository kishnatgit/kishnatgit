const addTowNumvers = (a, b) => a + b;

addTowNumvers(5, 5);

const array = [1, 2, 3, 4, 5];

function SecondLargest(array, greatest,secondgreatest) {
  array.forEach((i) => {
    if (i > greatest) {
      greatest = i;
    }
  });
  array.forEach((n)=>{
    if(n>secondgreatest && i<greatest){}
  });
  
}


